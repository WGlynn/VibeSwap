Building in public means showing the ugly parts too.

The failed tests. The reverted commits. The 3 AM "why does this compile but do the wrong thing" moments.

Follow along: github.com/WGlynn/VibeSwap

Every commit is public. Every struggle is documented. Every win is shared.

This is how trust is built. Not with promises. With proof.
