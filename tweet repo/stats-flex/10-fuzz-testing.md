We don't just test VibeSwap. We attack it.

41 fuzz test files throw random inputs at every contract. Edge cases. Overflow attempts. Weird ordering. Impossible parameters.

If a fuzz test can break it, we fix it before it ships.

"Works on my machine" isn't a test strategy. Random chaos is.
