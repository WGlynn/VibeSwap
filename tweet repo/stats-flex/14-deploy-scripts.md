5 deploy scripts. One command to go from zero to fully operational DEX.

Deploy.s.sol → Core contracts
ConfigurePeers.s.sol → LayerZero cross-chain setup
DeployOracle.s.sol → Price oracle network
DeployGovernance.s.sol → DAO + voting systems
DeployIncentives.s.sol → Shapley + rewards

Reproducible deployment. Every chain. Same state.
